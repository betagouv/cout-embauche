import themeColour from '../../themeColour'

export let answered = {
	background: themeColour,
	border: '1px solid ' + themeColour,
	color: 'white',
}

export let answer = {
	color: themeColour,
	border: '1px solid ' + themeColour,
}
